<!DOCTYPE html>
<html lang="fr">
<head>
    <title>{{ title }}</title>
</head>
<body>

<h1>{{ title }}</h1>

<ul>
    {% for user in users %}
        <li>{{ user.name }}</li>
    {% endfor %}
</ul>

</body>
</html>
